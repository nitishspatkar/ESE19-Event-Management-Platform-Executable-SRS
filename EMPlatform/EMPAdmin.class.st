Class {
	#name : #EMPAdmin,
	#superclass : #EMPUser,
	#category : #EMPlatform
}

{ #category : #accessing }
EMPAdmin >> approveServicePosting: aServicePosting [
	"If a service posting is valid (let's say its name, type and price aren't nill) then accept it- set 'accepted' flag to true"

	
]
